name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

env:
  PYTHONUTF8: "1"  # windows 러너 cp1252 콘솔에서 한국어 출력 UnicodeEncodeError 방지

jobs:
  selftest:
    # windows-latest: path_safety_gate 등 일부 게이트가 Windows 경로 의미론(드라이브 문자) 기반
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Doctor selftest (15 checks, GATE=GO required)
        run: python scripts/openbinggu_doctor.py --selftest

      - name: Public tree secret/PII scan (verdict=CLEAN required)
        run: python scripts/openbinggu_public_tree_scan.py --tree .
