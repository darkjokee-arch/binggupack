# V2-1 — save-intent durable inbox worker. 로컬 wrangler dev 한정.
# deploy 영구 금지 (V2-4 owner 명시 GO 전) — name 분리(오배포해도 라이브 무영향).
# 실행: scripts/openbinggu_save_intent_v21_selftest.py 가 dev 기동+키 주입+게이트 일괄 수행.
name = "binggupack-save-intent-v2-local"
main = "src/index.save_v2.ts"
compatibility_date = "2025-05-01"

[[durable_objects.bindings]]
name = "INBOX"
class_name = "IntentInbox"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["IntentInbox"]
